<Window x:Class="RebEasy.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RebEasy"
        Width="1280"
        Height="820"
        MinWidth="960"
        MinHeight="640"
        Background="#F4F1E8">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" />
            <ColumnDefinition Width="16" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.ColumnSpan="3"
                Padding="24"
                Background="#183A37"
                CornerRadius="24">
            <DockPanel LastChildFill="False">
                <StackPanel>
                    <TextBlock FontSize="30"
                               FontWeight="Bold"
                               Foreground="#F7F3EB"
                               Text="RebEasy" />
                    <TextBlock Margin="0,6,0,0"
                               Foreground="#D7E3D4"
                               Text="Leitura local e sincronizacao incremental da caixa Gmail." />
                    <TextBlock Margin="0,10,0,0"
                               Foreground="#D7E3D4"
                               Text="Conecte com um clique e continue direto para o consentimento Google." />
                </StackPanel>

                <StackPanel DockPanel.Dock="Right"
                            Orientation="Horizontal">
                    <Button x:Name="RefreshButton"
                            MinWidth="140"
                            Padding="18,10"
                            Margin="0,0,12,0"
                            IsEnabled="False"
                            Click="RefreshButton_OnClick"
                            Content="Atualizar" />
                    <Button x:Name="ConnectButton"
                            MinWidth="180"
                            Padding="18,10"
                            Click="ConnectButton_OnClick"
                            Content="Conectar Gmail" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="2"
                Padding="20"
                Background="#EFE7D8"
                CornerRadius="20">
            <DockPanel>
                <TextBox x:Name="SearchTextBox"
                         DockPanel.Dock="Top"
                         Margin="0,0,0,12"
                         Padding="10"
                         IsEnabled="False"
                         TextChanged="SearchTextBox_OnTextChanged" />

                <ListBox x:Name="MessagesList"
                         DisplayMemberPath="Subject"
                         SelectionChanged="MessagesList_OnSelectionChanged" />
            </DockPanel>
        </Border>

        <Border Grid.Row="2"
                Grid.Column="2"
                Padding="24"
                Background="#FFFDF8"
                CornerRadius="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="16" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock x:Name="SubjectText"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#1F1F1F"
                           Text="Selecione um e-mail" />

                <TextBlock x:Name="MetaText"
                           Grid.Row="1"
                           Margin="0,8,0,0"
                           Foreground="#5C5C5C"
                           Text="Os detalhes da mensagem aparecem aqui." />

                <TextBlock x:Name="StatusText"
                           Grid.Row="1"
                           Margin="0,32,0,0"
                           Foreground="#6F7B7A"
                           Text="Aguardando conexao. Clique em Conectar Gmail para abrir o login Google." />

                <ScrollViewer Grid.Row="3"
                              VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="BodyText"
                               TextWrapping="Wrap"
                               Foreground="#252525"
                               Text="Conecte a conta e rode a primeira sincronizacao para preencher o cache local." />
                </ScrollViewer>

                <StackPanel Grid.Row="4"
                            Orientation="Horizontal">
                    <Button Margin="0,0,8,0"
                            Padding="14,8"
                            Content="Exportar PDF"
                            IsEnabled="False" />
                    <Button Margin="0,0,8,0"
                            Padding="14,8"
                            Content="Exportar CSV"
                            IsEnabled="False" />
                    <Button Padding="14,8"
                            Content="Exportar JSON"
                            IsEnabled="False" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
