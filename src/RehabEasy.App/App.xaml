<Application x:Class="RehabEasy.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- Paleta clinica -->
        <Color x:Key="MedPrimaryColor">#0E6B7A</Color>
        <Color x:Key="MedPrimaryDarkColor">#0A5561</Color>
        <Color x:Key="MedPrimaryLightColor">#E6F2F4</Color>
        <Color x:Key="MedAccentColor">#1A8FA5</Color>
        <Color x:Key="MedDangerColor">#B54747</Color>
        <Color x:Key="MedDangerDarkColor">#943838</Color>
        <Color x:Key="MedWarningColor">#C7772E</Color>
        <Color x:Key="MedBackgroundColor">#E8EEF2</Color>
        <Color x:Key="MedSurfaceColor">#FFFFFF</Color>
        <Color x:Key="MedSurfaceMutedColor">#F4F8FA</Color>
        <Color x:Key="MedBorderColor">#C5D4DC</Color>
        <Color x:Key="MedTextPrimaryColor">#1A2B33</Color>
        <Color x:Key="MedTextSecondaryColor">#5A7180</Color>

        <SolidColorBrush x:Key="MedPrimaryBrush" Color="{StaticResource MedPrimaryColor}" />
        <SolidColorBrush x:Key="MedPrimaryDarkBrush" Color="{StaticResource MedPrimaryDarkColor}" />
        <SolidColorBrush x:Key="MedPrimaryLightBrush" Color="{StaticResource MedPrimaryLightColor}" />
        <SolidColorBrush x:Key="MedAccentBrush" Color="{StaticResource MedAccentColor}" />
        <SolidColorBrush x:Key="MedDangerBrush" Color="{StaticResource MedDangerColor}" />
        <SolidColorBrush x:Key="MedDangerDarkBrush" Color="{StaticResource MedDangerDarkColor}" />
        <SolidColorBrush x:Key="MedWarningBrush" Color="{StaticResource MedWarningColor}" />
        <SolidColorBrush x:Key="MedBackgroundBrush" Color="{StaticResource MedBackgroundColor}" />
        <SolidColorBrush x:Key="MedSurfaceBrush" Color="{StaticResource MedSurfaceColor}" />
        <SolidColorBrush x:Key="MedSurfaceMutedBrush" Color="{StaticResource MedSurfaceMutedColor}" />
        <SolidColorBrush x:Key="MedBorderBrush" Color="{StaticResource MedBorderColor}" />
        <SolidColorBrush x:Key="MedTextPrimaryBrush" Color="{StaticResource MedTextPrimaryColor}" />
        <SolidColorBrush x:Key="MedTextSecondaryBrush" Color="{StaticResource MedTextSecondaryColor}" />

        <!-- Tipografia -->
        <Style x:Key="MedPageTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource MedTextPrimaryBrush}" />
        </Style>

        <Style x:Key="MedSectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="17" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource MedTextPrimaryBrush}" />
        </Style>

        <Style x:Key="MedLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource MedTextSecondaryBrush}" />
        </Style>

        <Style x:Key="MedBody" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="{StaticResource MedTextPrimaryBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="MedCaption" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{StaticResource MedTextSecondaryBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="MedMetricValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="34" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{StaticResource MedPrimaryBrush}" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>

        <Style x:Key="MedMetricLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{StaticResource MedTextSecondaryBrush}" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>

        <!-- Cartoes -->
        <Style x:Key="MedCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource MedSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MedBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="20" />
        </Style>

        <Style x:Key="MedCardMuted" TargetType="Border" BasedOn="{StaticResource MedCard}">
            <Setter Property="Background" Value="{StaticResource MedSurfaceMutedBrush}" />
        </Style>

        <!-- Botao base -->
        <Style x:Key="MedButtonBase" TargetType="Button">
            <Setter Property="MinHeight" Value="42" />
            <Setter Property="MinWidth" Value="120" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.85" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MedButtonPrimary" TargetType="Button" BasedOn="{StaticResource MedButtonBase}">
            <Setter Property="Background" Value="{StaticResource MedPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MedPrimaryDarkBrush}" />
            <Setter Property="Foreground" Value="White" />
        </Style>

        <Style x:Key="MedButtonSecondary" TargetType="Button" BasedOn="{StaticResource MedButtonBase}">
            <Setter Property="Background" Value="{StaticResource MedSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MedBorderBrush}" />
            <Setter Property="Foreground" Value="{StaticResource MedTextPrimaryBrush}" />
        </Style>

        <Style x:Key="MedButtonAccent" TargetType="Button" BasedOn="{StaticResource MedButtonBase}">
            <Setter Property="Background" Value="{StaticResource MedAccentBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MedPrimaryDarkBrush}" />
            <Setter Property="Foreground" Value="White" />
        </Style>

        <Style x:Key="MedButtonDanger" TargetType="Button" BasedOn="{StaticResource MedButtonBase}">
            <Setter Property="Background" Value="{StaticResource MedSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MedDangerBrush}" />
            <Setter Property="Foreground" Value="{StaticResource MedDangerBrush}" />
        </Style>

        <!-- Campos -->
        <Style x:Key="MedTextBox" TargetType="TextBox">
            <Setter Property="MinHeight" Value="42" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Foreground" Value="{StaticResource MedTextPrimaryBrush}" />
            <Setter Property="Background" Value="{StaticResource MedSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MedBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="MedTextBoxMultiline" TargetType="TextBox" BasedOn="{StaticResource MedTextBox}">
            <Setter Property="MinHeight" Value="120" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="AcceptsTab" Value="True" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
        </Style>

        <Style x:Key="MedComboBox" TargetType="ComboBox">
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="MinWidth" Value="190" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Background" Value="{StaticResource MedSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MedBorderBrush}" />
        </Style>

        <!-- Abas sidebar (estilo web / segmented control) -->
        <Style x:Key="MedSidebarTabControl" TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Padding="3"
                                    Background="{StaticResource MedSurfaceMutedBrush}"
                                    BorderBrush="{StaticResource MedBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8">
                                <TabPanel IsItemsHost="True" />
                            </Border>
                            <Border Grid.Row="1"
                                    Margin="0,10,0,0"
                                    Background="Transparent">
                                <ContentPresenter ContentSource="SelectedContent" />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MedSidebarTabItem" TargetType="TabItem">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource MedTextSecondaryBrush}" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource MedSurfaceBrush}" />
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="{StaticResource MedBorderBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource MedPrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource MedTextPrimaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Abas -->
        <Style x:Key="MedTabControl" TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
        </Style>

        <Style x:Key="MedTabItem" TargetType="TabItem">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource MedTextSecondaryBrush}" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="0,0,0,3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="{StaticResource MedPrimaryBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource MedPrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource MedTextPrimaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Lista -->
        <Style x:Key="MedListBox" TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource MedSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MedBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="4" />
        </Style>

        <Style x:Key="MedListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Margin" Value="0,2" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource MedPrimaryLightBrush}" />
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{StaticResource MedPrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#F0F6F8" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Barras de progresso -->
        <Style x:Key="MedProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="12" />
            <Setter Property="Foreground" Value="{StaticResource MedPrimaryBrush}" />
            <Setter Property="Background" Value="#D9E6EB" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

    </Application.Resources>
</Application>
